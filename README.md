# OsteoDetect CI/CD

Автоматизация тестирования и развертывания ML веб-приложения для анализа изображений клеточных колоний.

## 🚀 Краткое описание

Этот проект настраивает CI/CD пайплайн для Flask веб-приложения с машинным обучением. Приложение классифицирует изображения клеточных колоний на основе F-актина и развертывается автоматически при каждом обновлении кода.

## 🛠 Технологии

- **Backend:** Python 3.9+, Flask
- **ML:** TensorFlow/Keras
- **Контейнеризация:** Docker
- **CI/CD:** GitHub Actions
- **Тестирование:** pytest, unittest

## 📦 Быстрый старт

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd osteodetect-ci-cd
```
2. **Установка зависимостей:**

```bash
pip install -r requirements.txt`
```
3. **Запуск локально:**

```bash
python run.py
```
Приложение будет доступно по адресу: http://localhost:5000

4. **Локальный деплой и тестирование:**

```bash
python deploy_local.py
```
Этот скрипт полностью проверит сборку, запуск и работу приложения.

## Запуск в Docker:

```bash
docker build -t osteodetect .
docker run -p 5000:5000 osteodetect
```
## 🧪 Тестирование
Запуск всех тестов:

```bash
python run_all_tests.py
```
Или отдельных категорий:

```bash
python run_all_tests.py unit     # Только unit-тесты
python run_all_tests.py api      # Только API-тесты
python run_all_tests.py ml       # Только ML-тесты
```
## 🔄 CI/CD Пайплайн
При пуше в ветки main или develop автоматически запускается:

Проверка качества кода (линтеры)

Запуск всех тестов (unit, API, ML, mock)

Сборка Docker-образа

Деплой приложения

## 🐳 Docker
**Для разработки:**

```bash
docker-compose up
```
**Для production:**

```bash
docker build -t osteodetect-prod -f Dockerfile .
```
## 📁 Структура проекта
```text
├── app/              # Исходный код приложения
├── tests/            # Тесты всех типов
├── github/workflows/ # CI/CD конфигурации
├── static/           # Статические файлы
├── templates/        # HTML шаблоны
└── deployment_logs/  # Логи деплоя
```
## 🌐 Использование
Откройте http://localhost:5000 в браузере

Загрузите изображение клеточной колонии

Получите результат классификации

## 📊 Endpoints
GET / - Главная страница с интерфейсом

GET /health - Проверка статуса приложения

POST /predict - Классификация изображения (JSON с base64)
